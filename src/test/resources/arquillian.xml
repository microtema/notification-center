<?xml version="1.0" encoding="UTF-8"?>
<arquillian xmlns="http://jboss.org/schema/arquillian"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="
        http://jboss.org/schema/arquillian
        http://jboss.org/schema/arquillian/arquillian_1_0.xsd">

    <!--defaultProtocol type="Servlet 3.0"/-->
    <container qualifier="jbossas" default="true">
        <configuration>
            <!-- configuration that starts JBoss at ports + 100 (8180 instead of 8080). -->
            <!-- This allows us to run this JBoss in parallel with another development JBoss -->
            <!-- Thus making running our tests a lot more failsafe and simpler. -->
            <!-- If you want to use an existing JBoss AS installation, change the value of this property to that path -->
            <!-- If you've already set the JBOSS_HOME environment variable, you can remove this configuration block -->
            <property name="jbossHome">target/jboss-as-7.1.1.Final</property>
            <!--property name="javaVmArguments">-Xmx512m -XX:MaxPermSize=128m -Xverify:none -XX:+UseFastAccessorMethods</property-->
            <property name="javaVmArguments">-Xms1024m -Xmx2048m -XX:PermSize=512M -XX:MaxPermSize=512 -XX:+CMSClassUnloadingEnabled -Djboss.socket.binding.port-offset=100 -Djboss.management.native.port=9054</property>
            <property name="outputToConsole">true</property>
            <!-- This is important for Arquillian to know which JBoss to use.. -->
            <property name="managementPort">9154</property>
        </configuration>
    </container>

</arquillian>
